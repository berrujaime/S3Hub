// Shared cache configuration constants.
// Pure module: no React, Expo, or AWS SDK imports.

// Cache expiration time: 1 week (in milliseconds).
export const CACHE_EXPIRATION = 7 * 24 * 60 * 60 * 1000;

// Number of items loaded per page in the file list.
export const PAGE_SIZE = 10;
